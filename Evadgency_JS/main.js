// main.js — single entry point for the Rollup build.
// Rollup needs one starting file to trace all imports from.
// We import both top-level modules here so everything gets included.
import './gameLoop.js';
import './menuFunctions.js';
